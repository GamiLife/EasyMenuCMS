import styled from '@emotion/styled';
import { Container } from '@gamiui/standard';

export const CreateBase = styled(Container)`
  display: flex;
  flex-direction: column;
  height: 100%;
`;

export const HeaderCreate = styled(Container)``;
