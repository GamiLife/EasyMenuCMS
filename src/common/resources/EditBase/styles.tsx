import styled from '@emotion/styled';
import { Container } from '@gamiui/standard';

export const EditBase = styled(Container)`
  display: flex;
  flex-direction: column;
  height: 100%;
`;

export const HeaderEdit = styled(Container)``;
