import styled from '@emotion/styled';
import { Container, Loader } from '@gamiui/standard';

export const LoaderListTable = styled(Loader.Wrapper)`
  height: 291px;
`;

export const Actions = styled(Container)`
  display: flex;
  gap: 1rem;
`;
