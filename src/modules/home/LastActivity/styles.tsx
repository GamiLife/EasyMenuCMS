import styled from '@emotion/styled';
import { Container, Title } from '@gamiui/standard';

export const LastActivity = styled(Container)`
  margin-top: 1rem;
`;

export const ActivityTitle = styled(Title)``;
